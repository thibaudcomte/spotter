import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spotter',
  webDir: 'dist/spotter/browser'
};

export default config;
